version: "3"
services:
  api:
    build:
      context: .
    ports:
      - "8912:8912"
    volumes:
      - .:/home
  db:
    image: "postgres"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: $DATABASE_PASSWORD
    restart: always
